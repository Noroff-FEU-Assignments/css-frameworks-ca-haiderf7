{
  "devDependencies": {
    "live-server": "^1.1.0",
    "sass": "^1.58.3"
  },
  "scripts": {
    "build": "sass scss:css",
    "watch": "sass --watch scss:css & live-server"
  },
  "dependencies": {
    "bootstrap": "^4.6.0"
  }
}
